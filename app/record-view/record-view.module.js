/**
 * Top level module for the record view systems within the app. Should contain all dependencies for the record
 * view functionality.
 */
(function() {
    'use strict';

    angular.module('introApp.recordView', []);
})();
